# Organize content by creating a directory and placing a file inside.
# - Define the name of the folder.
# - Create the folder if it doesn’t exist.
# - Move into that folder.
# - Create a new empty file inside it

mkdir new_folder
echo "new folder created"
my_dir="new_folder"
cd new_folder
touch new_file.txt
